## Skill development

`./skills/` is the source of truth for locally maintained skills.

- Create and modify skills under `./skills/`.
- Never edit installed copies under `./.agents/skills/` directly.
- If the requested skill does not exist under `./skills/`, ask the user how to proceed.
- After changing a skill, install or update it using `npx skills add`.
